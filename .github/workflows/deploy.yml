name: GCP Build + Mirror Secrets + Push (DISABLED)

# This workflow references services/ paths that don't exist.
# Deployment is handled by baby.yml via SSH.
# Re-enable this only if moving to GCP Artifact Registry + Cloud Run.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type ENABLE to run this workflow"
        required: true

jobs:
  build-and-push:
    if: ${{ github.event.inputs.confirm == 'ENABLE' }}
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "GCP build pipeline not yet configured. Use baby.yml for deployment."
